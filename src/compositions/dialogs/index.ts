/* eslint-disable import/extensions */
import { BcgLoginDialog } from './login-dialog';

import { componentNames } from '../../utils/config';

customElements.define(componentNames.compositions.dialogs.login, BcgLoginDialog);
