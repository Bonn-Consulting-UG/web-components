/* eslint-disable import/extensions */
import { BcgContactSubmission } from './contact-submission';

import { componentNames } from '../../utils/config';

customElements.define(
  componentNames.compositions.contactSubmission,
  BcgContactSubmission
);
