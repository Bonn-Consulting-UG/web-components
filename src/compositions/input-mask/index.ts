/* eslint-disable import/extensions */
import { BcgInputMask } from './input-mask';

import { componentNames } from '../../utils/config';

window.customElements.define(
  componentNames.compositions.inputMask,
  BcgInputMask
);
