/* eslint-disable import/extensions */
import { SubmissionCard } from './submisson-card';
import { componentNames } from '../../utils/config';

customElements.define(componentNames.compositions.submissionCard, SubmissionCard);
