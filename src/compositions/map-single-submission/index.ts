/* eslint-disable import/extensions */
import { BcgMapSingleSubmission } from './map-single-submission';

import { componentNames } from '../../utils/config';

customElements.define(
  componentNames.compositions.mapSingleSubmission,
  BcgMapSingleSubmission
);
