/* eslint-disable import/extensions */
import { BcgInputAmount } from './component';
import { tagPrefix } from '../../config';

window.customElements.define(`${tagPrefix}-input-amount`, BcgInputAmount);
