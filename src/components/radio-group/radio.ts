import { css, html, LitElement, ScopedElementsMixin } from '@lion/core';
import { LionRadioGroup, LionRadio } from '@lion/radio-group';

export class BcgRadio extends LionRadio {
  static get styles() {
    return [
      css`

      }`,
    ];
  }
}
