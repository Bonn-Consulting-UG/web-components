import { css } from '@lion/core';
import { LionPagination } from '@lion/pagination';

export class BcgPagination extends LionPagination {
  static get styles() {
    return [
      ...super.styles,
      css`

      }`,
    ];
  }

}
