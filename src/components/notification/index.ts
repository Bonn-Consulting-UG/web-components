/* eslint-disable import/extensions */
import { BcgNotification } from './notification';
import { componentNames } from '../../utils/config';

customElements.define(componentNames.notification, BcgNotification);
