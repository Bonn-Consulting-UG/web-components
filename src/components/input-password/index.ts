/* eslint-disable import/extensions */
import { BcgInputPassword } from './input-password';
import { componentNames } from '../../utils/config';

customElements.define(componentNames.inputpassword, BcgInputPassword);
